// Generated by the Freon Language Generator.
// TEMPLATE ConceptTemplate.generateConceptPrivate(...)
// ... and customized for the test !

import { observablepart, type FreExpressionNode, FreNodeReference } from "../../../ast";
import { FreParseLocation } from '../../../reader';
import { CalcExpression, InputField } from "./internal.js";

/**
 * Class InputFieldReference is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class InputFieldReference extends CalcExpression implements FreExpressionNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<InputFieldReference>): InputFieldReference {
        const result = new InputFieldReference(data.$id);
        if (!!data.field) {
            result.field = data.field;
        }
        if (!!data.parseLocation) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "InputFieldReference"; // holds the metatype in the form of a string

    parseLocation: FreParseLocation; // if relevant, the location of this element within the source from which it is parsed

    field: FreNodeReference<InputField>; // implementation of reference 'field'

    constructor(id?: string) {
        super(id);

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "field");
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return true;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): InputFieldReference {
        const result = new InputFieldReference();
        if (!!this.field) {
            result.field = this.field.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<InputFieldReference>): boolean {
        let result: boolean = super.match(toBeMatched);
        if (result && !!toBeMatched.field) {
            result = result && this.field.match(toBeMatched.field);
        }
        return result;
    }

    /**
     * Convenience method for reference 'field'.
     * Instead of returning a 'FreNodeReference<InputField>' object,
     * it returns the referred 'InputField' object, if it can be found.
     */
    get $field(): InputField {
        if (!!this.field) {
            return this.field.referred;
        }
        return null;
    }
}
