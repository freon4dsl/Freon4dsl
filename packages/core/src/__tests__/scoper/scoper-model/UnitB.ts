// Generated by the Freon Language Generator.
// TEMPLATE: UnitTemplate.generateUnit(...)

// import {
//     MobxModelElementImpl,
//     observableprim,
//     observablepartlist,
//     type FreModelUnit,
//     FreParseLocation,
//     FreNodeReference,
//     FreUtils,
//     matchElementList,
//     matchReferenceList,
// } from "@freon4dsl/core";
import { type IWithoutName, type IWithName } from "./internal.js";
import {
    FreModelUnit,
    FreNodeReference,
    MobxModelElementImpl,
    observablepartlist,
    observableprim
} from '../../../ast/index.js';
import { FreParseLocation } from '../../../reader/index.js';
import { FreUtils } from '../../../util/index.js';
import { matchElementList, matchReferenceList } from '../../../ast-utils/index.js';

import { makeObservable, action } from "mobx";

/**
 * Class UnitB is the implementation of the model unit with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to any changes in the state of its properties.
 */
export class UnitB extends MobxModelElementImpl implements FreModelUnit, IWithName {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<UnitB>): UnitB {
        const result = new UnitB(data.$id);
        if (!!data.name) {
            result.name = data.name;
        }
        if (!!data.childrenNoName) {
            data.childrenNoName.forEach((x) => result.childrenNoName.push(x));
        }
        if (!!data.childrenWithName) {
            data.childrenWithName.forEach((x) => result.childrenWithName.push(x));
        }
        if (!!data.myRef) {
            data.myRef.forEach((x) => result.myRef.push(x));
        }
        if (!!data.parseLocation) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    fileExtension: string = "";
    readonly $typename: string = "UnitB"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation; // if relevant, the location of this element within the source from which it is parsed
    name: string; // implementation of name
    childrenNoName: IWithoutName[]; // implementation of part 'childrenNoName'
    childrenWithName: IWithName[]; // implementation of part 'childrenWithName'
    myRef: FreNodeReference<IWithName>[]; // implementation of reference 'myRef'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }
        // Both 'observableprim' and 'observableprimlist' change the get and set of the attribute
        // such that the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observableprim(this, "name");
        this.name = "";

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepartlist(this, "childrenNoName");
        observablepartlist(this, "childrenWithName");
        observablepartlist(this, "myRef");

        // Make copy method a mobx action
        makeObservable(this, {
            copy: action,
        });
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return true;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): UnitB {
        const result = new UnitB();
        if (!!this.name) {
            result.name = this.name;
        }
        if (!!this.childrenNoName) {
            this.childrenNoName.forEach((x) => result.childrenNoName.push(x.copy()));
        }
        if (!!this.childrenWithName) {
            this.childrenWithName.forEach((x) => result.childrenWithName.push(x.copy()));
        }
        if (!!this.myRef) {
            this.myRef.forEach((x) => result.myRef.push(x.copy()));
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<UnitB>): boolean {
        let result: boolean = true;
        if (result && toBeMatched.name !== null && toBeMatched.name !== undefined && toBeMatched.name.length > 0) {
            result = result && this.name === toBeMatched.name;
        }
        if (result && !!toBeMatched.childrenNoName) {
            result = result && matchElementList(this.childrenNoName, toBeMatched.childrenNoName);
        }
        if (result && !!toBeMatched.childrenWithName) {
            result = result && matchElementList(this.childrenWithName, toBeMatched.childrenWithName);
        }
        if (result && !!toBeMatched.myRef) {
            result = result && matchReferenceList(this.myRef, toBeMatched.myRef);
        }
        return result;
    }
}
