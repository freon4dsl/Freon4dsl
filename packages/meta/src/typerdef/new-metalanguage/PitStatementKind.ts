// Generated by the ProjectIt Language Generator.
import { PiLangElement } from "../../languagedef/metalanguage";
import { ParseLocation } from "../../utils";

/**
 * Class PitStatementKind is the implementation of the limited concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to changes in the state of its properties.
 */
export class PitStatementKind implements PiLangElement {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data
     */
    static create(data: Partial<PitStatementKind>): PitStatementKind {
        const result = new PitStatementKind();
        if (!!data.name) {
            result.name = data.name;
        }
        return result;
    }

    static EQUALS: PitStatementKind; // implementation of instance EQUALS
    static CONFORMS: PitStatementKind; // implementation of instance CONFORMS
    static $piANY: PitStatementKind; // default predefined instance

    name: string = "";
    location: ParseLocation; // implementation of name

    toPiString(): string {
        return this.name;
    }
}

// Because of mobx we need to generate the initialisations outside of the class,
// otherwise the state of properties with primitive type will not be kept correctly.
PitStatementKind.EQUALS = PitStatementKind.create({
    name: "equalsto"
});
PitStatementKind.CONFORMS = PitStatementKind.create({
    name: "conformsto"
});

