// Generated by the Freon Language Generator.

import { FretTypeRule } from "./FretTypeRule";

/**
 * Class FretEqualsRule is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to any changes in the state of its properties.
 */
export class FretEqualsRule extends FretTypeRule {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data
     */
    static create(data: Partial<FretEqualsRule>): FretEqualsRule {
        const result = new FretEqualsRule();
        if (!!data.exp) {
            result.exp = data.exp;
        }
        if (data.agl_location) {
            result.agl_location = data.agl_location;
        }
        return result;
    }

    readonly $typename: string = "FretEqualsRule"; // holds the metatype in the form of a string
    toFreString(): string {
        return "equalsto " + this.exp.toFreString() + ";";
    }
}
