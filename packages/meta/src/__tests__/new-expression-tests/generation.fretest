expressions for language test1 TRANSLATED into TypeScript

AA {
    // the possibilities for primitive properties
    self.AAprop1; // : string;
    ==> this.AAprop1, rest idem
    self.AAprop2; // : string[];
    self.AAprop3; // : number;
    self.AAprop4; // : number[];
    self.AAprop5; // : boolean;
    self.AAprop6; // : boolean[];
    // properties with concept as type
    self.AAprop7; // : BB;
    self.AAprop8; // : BB[];
    self.AAprop9; // : CC; // reference
    self.AAprop10; // : CC[]; // reference
    // properties with interface as type
    self.AAprop11; // : DD;
    self.AAprop12; // : DD[];
    self.AAprop13; // : FF; // reference
    self.AAprop14; // : FF[]; // reference
}

BB { // properties with limited concepts as type
    self.BBprop15; // : ZZ;
    ===> this.BBprop15, rest idem
    self.BBprop16; // : ZZ[];
    BBprop17; // : reference ZZ;
    ===> this.BBprop15, rest idem 'this' REQUIRED
    BBprop18; // : reference ZZ[];
}

CC {
    #ZZ:instanceZZ1;
        ===> ZZ.instanceZZ1
}

DD {
    self.DDprop7.type ( );
    ===> LanguageEnvironment.getInstance().type.inferType( this.DDprop7 )
    owner()
    ===> this.freOwner()
    owner().if(BB)
    ===> (this.freOwner() as BB)
    owner().if(BB).type (  );
    ===> LanguageEnvironment.getInstance().type.inferType( (this.freOwner() as BB) )
}

FF {
    self.ee.dd.cc.bb.aa;
}
