import { PiEditorStyle, PiStyle } from "@projectit/core";

export const editorStyle: PiEditorStyle = {
// Add global style here:
    global: {
        light: {
        },
        dark: {
        }
    },
// Add concept styles here:
}
