// import { JsonArray, JsonFile, JsonObject, JsonPair } from "../language/gen";

export class JsonInitialization {

    initialize() {
        // const result = new JsonFile();
        // const obj = new JsonObject();
        // result.contents = obj;
        // const pair = new JsonPair();
        // obj.pairs.push(pair);
        // pair.name= "jojo";
        // pair.value = new JsonArray()
        //
        // return result;
    }
}
