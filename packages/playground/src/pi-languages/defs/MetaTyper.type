typer xxx for language PiLanguage

istype { PiClassifier }

PitPropertyRef {
    infertype self.p.type; // TODO error message when this expression does not result in one of the types mentioned in istype{}
}
PitSelfExp {
    infertype ancestor(PitClassifierRule).myClassifier;
}

PitFunctionCallExp {
    infertype self.returnType;
}

PitWhereExp {
    infertype self.otherType.type;
}
