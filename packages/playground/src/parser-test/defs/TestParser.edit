editor XXX for language TestParser

//VisibilityKind {
//    @projection normal [
//        ${PUBLIC @keyword [+]}
//        ${PRIVATE @keyword [-]}
//        ${PROTECTED @keyword [#]}
//    ]
//}

PrimExtra {
    @projection normal [
    before ${self.primNumberWithExtra}: number;
    before ${self.primStringWithExtra}: string;
    before ${self.primBooleanWithExtra}: boolean;
//    before ${self.primListNumberWithExtra}: number\[\];
//    before ${self.primListStringWithExtra}: string\[\];
//    before ${self.primListBooleanWithExtra}: boolean\[\];
    ]
}
//
//PrimExtraOptional {
//    @projection normal [PrimExtraOptional
//    [?before ${self.primNumberWithExtra} after]
//    [?before ${self.primStringWithExtra} after]
//    [?before ${self.primBooleanWithExtra} after]
//    [?before ${self.primListNumberWithExtra} after]
//    [?before ${self.primListStringWithExtra} after]
//    [?before ${self.primListBooleanWithExtra} after]
//    ]
//}
