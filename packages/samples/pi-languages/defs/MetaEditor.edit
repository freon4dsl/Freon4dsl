editor First

PiEditorDef {
    [
    editor ${name} for language ${languageName}
    ${conceptEditors vertical}
    ]
}

PiEditConcept {
    [
    ${concept} {
    ${projection}
    [?trigger ${trigger}]
    [?symbol ${operator}]
    ]
}

