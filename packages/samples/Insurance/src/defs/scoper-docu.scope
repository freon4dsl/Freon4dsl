scoper DocuScoper for language InsuranceModel

isnamespace { InsuranceProduct, BaseProduct, CalcFunction, Entity }

InsuranceProduct {
    namespace_addition = self.basedOn;
}

AttributeRef {
	namespace_replacement = typeof( container );
}

Entity {
    namespace_addition = self.baseEntity;
}
