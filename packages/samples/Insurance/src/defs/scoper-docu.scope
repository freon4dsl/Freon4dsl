scoper for language InsuranceModel

isNamespace { InsuranceProduct, BaseProduct, CalcFunction, Entity }

InsuranceProduct {
    imports {self.basedOn;}
}

AttributeRef {
	alternatives { owner().type(); }
}

Entity {
    imports {self.baseEntity;}
}
