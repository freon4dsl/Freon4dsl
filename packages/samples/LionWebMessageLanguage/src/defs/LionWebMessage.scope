scoper for language LionWebMessage 

isNamespace { Types, MessageGroup, ObjectType }

// MessageGroup {
//     imports {
//         self.owner().typeDefinitions;
//     }
// }

ObjectType {
    alternatives {
        self.owner().if(MessageGroup).owner().typeDefinitions;
    }
}
