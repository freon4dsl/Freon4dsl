scoper for language ScopingExample

isNamespace { Block, Function, VariableRef }

/* step 0: show the model without scoper */
/* step 1 and 2 without ComplexVarRef!!! */
/* step 1: add Block as Namespace and show what can be referenced in various places */
/* step 2: add Function as Namespace and show what can be referenced in various places */

/* step 3: show imports */
Block {
    imports { self.imports; }
}
