scoper EducationScoper for language Education

isnamespace { Page, FlowDescription, FlowRule, Topic, Step, Test  }

FlowDescription {
    namespace_addition = self.topic;
}

Test {
    namespace_addition = self.topic;
}

Step {
    namespace_addition = self.fromPage;
}

//FlowRule {
////    namespace_addition = self.page;
//}
