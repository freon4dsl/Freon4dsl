// Generated by the Freon Language Generator.
import { FreError, FreErrorSeverity } from "@freon4dsl/core";
import { FlowDescription } from "../language/gen/index.js";
import { EducationDefaultWorker } from "../utils/gen/EducationDefaultWorker.js";
import { EducationCheckerInterface } from "./gen/EducationValidator.js";

export class CustomEducationValidator extends EducationDefaultWorker implements EducationCheckerInterface {
    errorList: FreError[] = [];

    override execAfterFlowDescription(modelelement: FlowDescription): boolean {
        if (!modelelement.name.startsWith("Flow")) {
            this.errorList.push(
                new FreError(
                    `wThe name of flow description '${modelelement.name}' should start with 'Flow'`,
                    modelelement,
                    "unnamed",
                    "name",
                    FreErrorSeverity.ToDo
                )
            );
        }
        return false;
    }
}
