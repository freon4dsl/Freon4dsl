scoper for language Example

// isNamespace { Entity,Model }

// isNamespace { Entity, Model }

// 2 isNamespace { Method, Entity }

isNamespace { Method, Entity, SumExpression }

Entity {
    imports { self.baseEntity; }
    // How to tell that there is no imports for self.baseEntity ??
    // imports { self.baseEntity; } for Type
    // NOT imports { self.baseEntity; } for Entity
}

AppliedFeature {
	alternatives { owner().type(); }
}


