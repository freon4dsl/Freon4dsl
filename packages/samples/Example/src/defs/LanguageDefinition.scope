scoper for language Example

// isnamespace { Entity,Model }

// isnamespace { Entity, Model }

// 2 isnamespace { Method, Entity }

isnamespace { Method, Entity, SumExpression }

Entity {
    import { self.baseEntity; }
    // How to tell that there is no import for self.baseEntity ??
    // import = self.baseEntity for Type
    // NOT import = self.baseEntity for Entity
}

AppliedFeature {
	alternative { typeof( container ); }
}


