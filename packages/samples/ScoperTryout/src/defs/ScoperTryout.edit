editor qualified

UnitHoldingReferables {[
    unitType1 ${self.name}
    ${content vertical};
]}

UnitUsingReferables {[
    unitType2 ${self.name}
    myReferences: ${self.myReferences vertical}
]}

QualifiedName {[
    Q ${self.part}[?.${self.restName}]
]}
