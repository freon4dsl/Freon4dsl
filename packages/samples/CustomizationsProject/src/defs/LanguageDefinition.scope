scoper Entity for language Entity

isnamespace { EntityModelUnit, Entity, EntityFunction }

Entity {
    namespace_addition = self.baseEntity;
}

/*
Type {
    namespace_addition = self.baseInterface
}
*/

AppliedFeature {
	namespace_replacement = typeof( container );
}
