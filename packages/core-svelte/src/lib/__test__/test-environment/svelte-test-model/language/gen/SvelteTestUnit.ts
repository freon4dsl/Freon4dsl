// Generated by the Freon Language Generator.
import {
    MobxModelElementImpl,
    observableprim,
    observablepartlist,
    type FreModelUnit,
    type FreParseLocation,
    type FreNodeReference,
    FreUtils,
    matchElementList,
    matchReferenceList,
} from "@freon4dsl/core";
import { SimpleNode, OtherType } from "./internal.js";

import { makeObservable, action } from "mobx";

/**
 * Class SvelteTestUnit is the implementation of the model unit with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to any changes in the state of its properties.
 */
export class SvelteTestUnit extends MobxModelElementImpl implements FreModelUnit {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<SvelteTestUnit>): SvelteTestUnit {
        const result = new SvelteTestUnit(data.$id);
        if (!!data.name) {
            result.name = data.name;
        }
        if (!!data.myList1) {
            data.myList1.forEach((x) => result.myList1.push(x));
        }
        if (!!data.myList2) {
            data.myList2.forEach((x) => result.myList2.push(x));
        }
        if (!!data.myList3) {
            data.myList3.forEach((x) => result.myList3.push(x));
        }
        if (!!data.myList4) {
            data.myList4.forEach((x) => result.myList4.push(x));
        }
        if (!!data.myList5) {
            data.myList5.forEach((x) => result.myList5.push(x));
        }
        if (!!data.myList6) {
            data.myList6.forEach((x) => result.myList6.push(x));
        }
        if (!!data.myList7) {
            data.myList7.forEach((x) => result.myList7.push(x));
        }
        if (!!data.myList8) {
            data.myList8.forEach((x) => result.myList8.push(x));
        }
        if (!!data.myList9) {
            data.myList9.forEach((x) => result.myList9.push(x));
        }
        if (!!data.parseLocation) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    fileExtension: string = "";
    readonly $typename: string = "SvelteTestUnit"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation!: FreParseLocation; // if relevant, the location of this element within the source from which it is parsed
    name: string; // implementation of name
    myList1!: SimpleNode[]; // implementation of part 'myList1'
    myList2!: SimpleNode[]; // implementation of part 'myList2'
    myList3!: OtherType[]; // implementation of part 'myList3'
    myList4!: SimpleNode[]; // implementation of part 'myList4'
    myList5!: SimpleNode[]; // implementation of part 'myList5'
    myList6!: OtherType[]; // implementation of part 'myList6'
    myList7!: FreNodeReference<SimpleNode>[]; // implementation of part 'myList7'
    myList8!: FreNodeReference<OtherType>[]; // implementation of part 'myList8'
    myList9!: FreNodeReference<OtherType>[]; // implementation of reference 'myList9'


    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }
        // Both 'observableprim' and 'observableprimlist' change the get and set of the attribute
        // such that the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observableprim(this, "name");
        this.name = "";

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepartlist(this, "myList1");
        observablepartlist(this, "myList2");
        observablepartlist(this, "myList3");
        observablepartlist(this, "myList4");
        observablepartlist(this, "myList5");
        observablepartlist(this, "myList6");
        observablepartlist(this, "myList7");
        observablepartlist(this, "myList8");
        observablepartlist(this, "myList9");

        // Make copy method a mobx action
        makeObservable(this, {
            copy: action,
        });
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return true;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): SvelteTestUnit {
        const result = new SvelteTestUnit();
        if (!!this.name) {
            result.name = this.name;
        }
        if (!!this.myList1) {
            this.myList1.forEach((x) => result.myList1.push(x.copy()));
        }
        if (!!this.myList2) {
            this.myList2.forEach((x) => result.myList2.push(x.copy()));
        }
        if (!!this.myList3) {
            this.myList3.forEach((x) => result.myList3.push(x.copy()));
        }
        if (!!this.myList4) {
            this.myList4.forEach((x) => result.myList4.push(x.copy()));
        }
        if (!!this.myList5) {
            this.myList5.forEach((x) => result.myList5.push(x.copy()));
        }
        if (!!this.myList6) {
            this.myList6.forEach((x) => result.myList6.push(x.copy()));
        }
        if (!!this.myList7) {
            this.myList7.forEach((x) => result.myList7.push(x.copy()));
        }
        if (!!this.myList8) {
            this.myList8.forEach((x) => result.myList8.push(x.copy()));
        }
        if (!!this.myList9) {
            this.myList9.forEach((x) => result.myList9.push(x.copy()));
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<SvelteTestUnit>): boolean {
        let result: boolean = true;
        if (result && toBeMatched.name !== null && toBeMatched.name !== undefined && toBeMatched.name.length > 0) {
            result = result && this.name === toBeMatched.name;
        }
        if (result && !!toBeMatched.myList1) {
            result = result && matchElementList(this.myList1, toBeMatched.myList1);
        }
        if (result && !!toBeMatched.myList2) {
            result = result && matchElementList(this.myList2, toBeMatched.myList2);
        }
        if (result && !!toBeMatched.myList3) {
            result = result && matchElementList(this.myList3, toBeMatched.myList3);
        }
        if (result && !!toBeMatched.myList4) {
            result = result && matchElementList(this.myList4, toBeMatched.myList4);
        }
        if (result && !!toBeMatched.myList5) {
            result = result && matchElementList(this.myList5, toBeMatched.myList5);
        }
        if (result && !!toBeMatched.myList6) {
            result = result && matchElementList(this.myList6, toBeMatched.myList6);
        }
        if (result && !!toBeMatched.myList7) {
            result = result && matchReferenceList(this.myList7, toBeMatched.myList7);
        }
        if (result && !!toBeMatched.myList8) {
            result = result && matchReferenceList(this.myList8, toBeMatched.myList8);
        }
        if (result && !!toBeMatched.myList9) {
            result = result && matchReferenceList(this.myList9, toBeMatched.myList9);
        }
        return result;
    }
}
