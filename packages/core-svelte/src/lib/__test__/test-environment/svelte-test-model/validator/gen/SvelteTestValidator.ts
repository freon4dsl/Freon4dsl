// Generated by the Freon Language Generator.
import { type FreValidator, FreError, FreCompositeTyper, type FreNode } from "@freon4dsl/core";
// import { FreNode } from "../../language/gen/index.js";
import { SvelteTestNonOptionalsChecker } from "./SvelteTestNonOptionalsChecker.js";

import { SvelteTestReferenceChecker } from "./SvelteTestReferenceChecker.js";
import { SvelteTestWalker, type SvelteTestWorker } from "../../utils/gen/index.js";
import { freonConfiguration } from "../../config/FreonConfiguration.js";

/**
 * Interface 'SvelteTestCheckerInterface' represents any object that traverses the model tree and checks
 * its nodes, where any errors are deposited in 'errorList'.
 * Every checker that is used by the validator 'SvelteTestValidator' should implement this interface.
 */
export interface SvelteTestCheckerInterface extends SvelteTestWorker {
    errorList: FreError[];
}

/**
 * Class SvelteTestValidator implements the validator generated from, if present, the validator definition,
 * otherwise this class implements the default validator.
 * The implementation uses the visitor pattern to traverse the tree. Class SvelteTestWalker implements
 * the actual checking of each node in the tree.
 */
export class SvelteTestValidator implements FreValidator {
    /**
     * Returns the list of errors found in 'modelelement'.
     * This method uses the visitor pattern to traverse the tree with 'modelelement' as top node,
     * where classes SvelteTestNonOptionalsChecker, SvelteTestReferenceChecker,  implements the actual checking of each node in the tree.
     *
     * @param modelelement
     * @param includeChildren if true, the children of 'modelelement' are also checked.
     * The default for 'includeChildren' is true.
     */
    public validate(modelelement: FreNode, includeChildren: boolean = true): FreError[] {
        // initialize the errorlist
        const errorlist: FreError[] = [];

        // create the walker over the model tree
        const myWalker = new SvelteTestWalker();

        // create the checker on non-optional parts
        let myChecker = new SvelteTestNonOptionalsChecker();
        myChecker.errorList = errorlist;
        // and add the checker to the walker
        myWalker.myWorkers.push(myChecker);

        // create the checker on references
        myChecker = new SvelteTestReferenceChecker();
        myChecker.errorList = errorlist;
        // and add the checker to the walker
        myWalker.myWorkers.push(myChecker);

        // add any custom validations
        for (let checker of freonConfiguration.customValidations) {
            checker.errorList = errorlist;
            myWalker.myWorkers.push(checker);
        }

        // do the work
        myWalker.walk(modelelement, () => {
            return includeChildren;
        });

        // return any errors
        return errorlist;
    }
}
