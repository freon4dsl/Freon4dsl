// Generated by the Freon Language Generator.
import { FreError, FreErrorSeverity, type FreWriter, FreLanguageEnvironment } from "@freon4dsl/core";
import { SvelteTest, SvelteTestUnit, SimpleNode, OtherType } from "../../language/gen/index.js";
import { SvelteTestDefaultWorker } from "../../utils/gen/index.js";
import type {SvelteTestCheckerInterface} from "./SvelteTestValidator.js";

/**
 * Class SvelteTestNonOptionalsChecker is part of the implementation of the default validator.
 * It checks whether non-optional properties, as such defined in the .ast definition, indeed
 * have a value.
 * Class SvelteTestWalker implements the traversal of the model tree. This class implements
 * the actual checking of each node in the tree.
 */
export class SvelteTestNonOptionalsChecker extends SvelteTestDefaultWorker implements SvelteTestCheckerInterface {
    // 'myWriter' is used to provide error messages on the nodes in the model tree
    myWriter: FreWriter = FreLanguageEnvironment.getInstance().writer;
    // 'errorList' holds the errors found while traversing the model tree
    errorList: FreError[] = [];

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeSvelteTest(node: SvelteTest): boolean {
        if (node.name === null || node.name === undefined || node.name?.length === 0) {
            this.errorList.push(
                new FreError("Property 'name' must have a value", node, node.name, FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeSvelteTestUnit(node: SvelteTestUnit): boolean {
        if (node.name === null || node.name === undefined || node.name?.length === 0) {
            this.errorList.push(
                new FreError("Property 'name' must have a value", node, node.name, FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeSimpleNode(node: SimpleNode): boolean {
        if (node.name === null || node.name === undefined || node.name?.length === 0) {
            this.errorList.push(
                new FreError("Property 'name' must have a value", node, node.name, FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeOtherType(node: OtherType): boolean {
        if (node.name === null || node.name === undefined || node.name?.length === 0) {
            this.errorList.push(
                new FreError("Property 'name' must have a value", node, node.name, FreErrorSeverity.Error),
            );
        }

        return false;
    }
}
