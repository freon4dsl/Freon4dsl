// Generated by the Freon Language Generator.
import { type Box, BoxUtil, FreBoxProvider, FreProjectionHandler, BoxFactory } from "@freon4dsl/core";

import { SvelteTestUnit } from "../../language/gen/index.js";

import { SvelteTestEnvironment } from "../../config/gen/SvelteTestEnvironment.js";

/**
 * This class implements the box provider for a single node of type SvelteTestUnit.
 * The box provider is able to create the (tree of) boxes for the node, based
 * on the projections that are currently selected by the user.
 * The top of the tree of boxes is always a box of type ElementBox, which is
 * a box that will never be rendered itself, only its content will. Thus, we
 * have a stable entry in the complete box tree for every FreNode node.
 */
export class SvelteTestUnitBoxProvider extends FreBoxProvider {
    constructor(mainHandler: FreProjectionHandler) {
        super(mainHandler);
        this.knownBoxProjections = ["default"];
        this.knownTableProjections = ["default"];
        this.conceptName = "SvelteTestUnit";
    }

    protected getContent(projectionName: string): Box {
        // console.log("GET CONTENT " + this._node?.freId() + ' ' +  this._node?.freLanguageConcept() + ' ' + projectionName);
        // see if we need to use a custom projection
        if (!this.knownBoxProjections.includes(projectionName) && !this.knownTableProjections.includes(projectionName)) {
            const BOX: Box = this.mainHandler.executeCustomProjection(this._node, projectionName);
            if (!!BOX) {
                // found one, so return it
                return BOX;
            }
        } else {
            // select the box to return based on the projectionName
            if (projectionName === "default") {
                return this.getDefault();
            }
        }
        // in all other cases, return the default
        return this.getDefault();
    }

    private getDefault(): Box {
        return BoxFactory.verticalLayout(this._node as SvelteTestUnit, "SvelteTestUnit-overall", "", [
            BoxFactory.horizontalLayout(
                this._node as SvelteTestUnit,
                "SvelteTestUnit-hlist-line-0",
                "",
                [
                    BoxUtil.labelBox(this._node as SvelteTestUnit, "SvelteTestUnit", "top-1-line-0-item-0"),
                    BoxUtil.textBox(this._node as SvelteTestUnit, "name"),
                    BoxUtil.labelBox(this._node as SvelteTestUnit, "\{", "top-1-line-0-item-2"),
                ],
                { selectable: false },
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                4,
                "1",
                BoxUtil.labelBox(this._node as SvelteTestUnit, "myList1", "top-1-line-1-item-0"),
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                8,
                "2",
                BoxUtil.verticalPartListBox(
                    this._node as SvelteTestUnit,
                    (this._node as SvelteTestUnit).myList1,
                    "myList1",
                    // @ts-ignore
                    null,
                    this.mainHandler,
                ),
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                4,
                "3",
                BoxUtil.labelBox(this._node as SvelteTestUnit, "myList2", "top-1-line-3-item-0"),
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                8,
                "4",
                BoxUtil.verticalPartListBox(
                    this._node as SvelteTestUnit,
                    (this._node as SvelteTestUnit).myList2,
                    "myList2",
                    // @ts-ignore
                    null,
                    this.mainHandler,
                ),
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                4,
                "5",
                BoxUtil.labelBox(this._node as SvelteTestUnit, "myList3", "top-1-line-5-item-0"),
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                8,
                "6",
                BoxUtil.verticalPartListBox(
                    this._node as SvelteTestUnit,
                    (this._node as SvelteTestUnit).myList3,
                    "myList3",
                    // @ts-ignore
                    null,
                    this.mainHandler,
                ),
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                4,
                "7",
                BoxUtil.labelBox(this._node as SvelteTestUnit, "myList4", "top-1-line-7-item-0"),
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                8,
                "8",
                BoxUtil.verticalPartListBox(
                    this._node as SvelteTestUnit,
                    (this._node as SvelteTestUnit).myList4,
                    "myList4",
                    // @ts-ignore
                    null,
                    this.mainHandler,
                ),
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                4,
                "9",
                BoxUtil.labelBox(this._node as SvelteTestUnit, "myList5", "top-1-line-9-item-0"),
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                8,
                "10",
                BoxUtil.verticalPartListBox(
                    this._node as SvelteTestUnit,
                    (this._node as SvelteTestUnit).myList5,
                    "myList5",
                    // @ts-ignore
                    null,
                    this.mainHandler,
                ),
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                4,
                "11",
                BoxUtil.labelBox(this._node as SvelteTestUnit, "myList6", "top-1-line-11-item-0"),
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                8,
                "12",
                BoxUtil.verticalPartListBox(
                    this._node as SvelteTestUnit,
                    (this._node as SvelteTestUnit).myList6,
                    "myList6",
                    // @ts-ignore
                    null,
                    this.mainHandler,
                ),
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                4,
                "13",
                BoxUtil.labelBox(this._node as SvelteTestUnit, "myList7", "top-1-line-13-item-0"),
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                8,
                "14",
                // @ts-ignore
                BoxUtil.verticalReferenceListBox(this._node as SvelteTestUnit, "myList7", SvelteTestEnvironment.getInstance().scoper, null),
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                4,
                "15",
                BoxUtil.labelBox(this._node as SvelteTestUnit, "myList8", "top-1-line-15-item-0"),
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                8,
                "16",
                // @ts-ignore
                BoxUtil.verticalReferenceListBox(this._node as SvelteTestUnit, "myList8", SvelteTestEnvironment.getInstance().scoper, null),
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                4,
                "17",
                BoxUtil.labelBox(this._node as SvelteTestUnit, "myList9", "top-1-line-17-item-0"),
            ),
            BoxUtil.indentBox(
                this._node as SvelteTestUnit,
                8,
                "18",
                // @ts-ignore
                BoxUtil.verticalReferenceListBox(this._node as SvelteTestUnit, "myList9", SvelteTestEnvironment.getInstance().scoper, null),
            ),
            BoxUtil.labelBox(this._node as SvelteTestUnit, "}", "top-1-line-19-item-0"),
        ]);
    }
}
