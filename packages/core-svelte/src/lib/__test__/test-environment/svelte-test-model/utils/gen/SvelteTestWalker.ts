// Generated by the Freon Language Generator.
// import { FreNode } from "../../language/gen/index.js";
import { OtherType, SimpleNode, SvelteTestUnit, SvelteTest } from "../../language/gen/index.js";
import type {SvelteTestWorker} from "./SvelteTestWorker.js";
import { FreLogger, type FreNode } from "@freon4dsl/core";

const LOGGER = new FreLogger("SvelteTestWalker");

/**
 * Class SvelteTestWalker implements the extended visitor pattern of instances of language SvelteTest.
 * This class implements the traversal of the model tree, classes that implement SvelteTestWorker
 * are responsible for the actual work being done on the nodes of the tree.
 * Every node is visited twice, once before the visit of its children, and once after this visit.
 *
 * With the use of the parameter 'includeChildren', which takes a function, a very fine-grained control can be taken
 * over which nodes are and are not visited.
 */
export class SvelteTestWalker {
    myWorkers: SvelteTestWorker[] = []; // the instances that do the actual work on each node of the tree

    /**
     * This method is the entry point of the traversal through the model tree. Each of the workers will be called in
     * the order in which they are present in the array 'myWorkers'. If, for a tree node, a worker returns 'false',
     * none of the rest of the workers will be called. For that particular node both the 'execBefore' and 'execAfter'
     * method of any of the other workers will be skipped.
     *
     * @param modelelement the top node of the part of the tree to be visited
     * @param includeChildren if true, the children of 'modelelement' will also be visited
     */
    public walk(modelelement: FreNode, includeChildren?: (elem: FreNode) => boolean) {
        if (this.myWorkers.length > 0) {
            if (modelelement instanceof OtherType) {
                return this.walkOtherType(modelelement, includeChildren);
            }
            if (modelelement instanceof SimpleNode) {
                return this.walkSimpleNode(modelelement, includeChildren);
            }
            if (modelelement instanceof SvelteTestUnit) {
                return this.walkSvelteTestUnit(modelelement, includeChildren);
            }
            if (modelelement instanceof SvelteTest) {
                return this.walkSvelteTest(modelelement, includeChildren);
            }
        } else {
            LOGGER.error("No worker found.");
        }
    }

    private walkOtherType(modelelement: OtherType, includeChildren?: (elem: FreNode) => boolean) {
        let stopWalkingThisNode: boolean = false;
        for (const worker of this.myWorkers) {
            if (!stopWalkingThisNode) {
                stopWalkingThisNode = worker.execBeforeOtherType(modelelement);
            }
        }

        for (let worker of this.myWorkers) {
            if (!stopWalkingThisNode) {
                stopWalkingThisNode = worker.execAfterOtherType(modelelement);
            }
        }
    }

    private walkSimpleNode(modelelement: SimpleNode, includeChildren?: (elem: FreNode) => boolean) {
        let stopWalkingThisNode: boolean = false;
        for (const worker of this.myWorkers) {
            if (!stopWalkingThisNode) {
                stopWalkingThisNode = worker.execBeforeSimpleNode(modelelement);
            }
        }

        for (let worker of this.myWorkers) {
            if (!stopWalkingThisNode) {
                stopWalkingThisNode = worker.execAfterSimpleNode(modelelement);
            }
        }
    }

    private walkSvelteTestUnit(modelelement: SvelteTestUnit, includeChildren?: (elem: FreNode) => boolean) {
        let stopWalkingThisNode: boolean = false;
        for (const worker of this.myWorkers) {
            if (!stopWalkingThisNode) {
                stopWalkingThisNode = worker.execBeforeSvelteTestUnit(modelelement);
            }
        }
        // work on children in the model tree
        modelelement.myList1.forEach((p) => {
            if (!(includeChildren === undefined) && includeChildren(p)) {
                this.walk(p, includeChildren);
            }
        });
        modelelement.myList2.forEach((p) => {
            if (!(includeChildren === undefined) && includeChildren(p)) {
                this.walk(p, includeChildren);
            }
        });
        modelelement.myList3.forEach((p) => {
            if (!(includeChildren === undefined) && includeChildren(p)) {
                this.walk(p, includeChildren);
            }
        });

        for (let worker of this.myWorkers) {
            if (!stopWalkingThisNode) {
                stopWalkingThisNode = worker.execAfterSvelteTestUnit(modelelement);
            }
        }
    }

    private walkSvelteTest(modelelement: SvelteTest, includeChildren?: (elem: FreNode) => boolean) {
        let stopWalkingThisNode: boolean = false;
        for (const worker of this.myWorkers) {
            if (!stopWalkingThisNode) {
                stopWalkingThisNode = worker.execBeforeSvelteTest(modelelement);
            }
        }
        // work on children in the model tree
        modelelement.units.forEach((p) => {
            if (!(includeChildren === undefined) && includeChildren(p)) {
                this.walk(p, includeChildren);
            }
        });

        for (let worker of this.myWorkers) {
            if (!stopWalkingThisNode) {
                stopWalkingThisNode = worker.execAfterSvelteTest(modelelement);
            }
        }
    }
}
