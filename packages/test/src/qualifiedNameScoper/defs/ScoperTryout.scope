scoper for language ScoperTryout

isnamespace { Referable, QualifiedName }

UnitType2 {
    namespace_addition { self.imports re_export; }
}

QualifiedName {
    namespace_replacement { container.part re_export; }
}
