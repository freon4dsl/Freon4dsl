scoper for language ScoperTryout

isnamespace { Referable, QualifiedName }

UnitType1 {
    import { self.imports ; }
}

UnitType2 {
    import { self.imports recursive; }
}

QualifiedName {
    alternative {
        owner().if(UnitType2) ;
        recursive owner().if(QualifiedName).part ;
    }
}
