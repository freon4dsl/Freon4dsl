scoper for language Demo

isnamespace { DemoModel, DemoEntity, DemoFunction, AppliedFeature }

// Inheritance scope
DemoEntity {
    import { self.baseEntity; }
}
/*
Type {
    import { self.baseInterface; }
}
*/
AppliedFeature {
	alternative { typeof( container ); }
}

//    t = typer.inferType(this.source) exclude: attributes where visibility = 'private'
//    include: if( t.friends.includes(context)) then t.attributes where visibility = 'friends'
