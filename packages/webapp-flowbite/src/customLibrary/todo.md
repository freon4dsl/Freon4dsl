# Todos in this branch
To help the memory, some things that still need to be done.

- [ ] check whether all replace boxes have an add and remove node method. 
- [ ] implement box.removeItem(index) and use it in HorizontalAccordion
- [ ] make components for all date/time options for both structured/string-based.
- [ ] determine how to do the CSS for all library components: leave them in svelte, or in tailwind, or...
- [ ] check VerticalAccordion on behavior and styles
- [ ] make grid component with sort mechanism - show Days or stages
- [ ] update the old custom components to not use AST.change()
- [ ] extend the .ast file for festival
- [ ] make generator for festival program (HTML or text format)
- [ ] add validations
- [ ] add email-input, phone-input, and url-input to the .edit file (use those from Graham)
